#include <iostream>
#include <vector>
using namespace std;

//Cannot use 2d arrays in c++ as column is not know, can use vectors though
//Probably runtime will be O(n^2)
boolean rotate(vector<vector<int> > &matrix)
{
	//Will come back to it, Solution will be made soon

}